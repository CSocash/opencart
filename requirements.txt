pytest
playwright
pytest-xdist
pytest-html
allure-pytest
pytest-rerunfailures
openpyxl
Faker
python-slugify


# to install these requirements in the virtual environment, run the following
# command in the terminal:
# pip install -r requirements.txt